apiVersion: v1
kind: Service
metadata:
    name: leaderboard
spec:
    progressDeadlineSeconds: 3600
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: leaderboard
---
apiVersion: v1
kind: Service
metadata:
    name: web
spec:
    progressDeadlineSeconds: 3600
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: web
    
